name: Deploy AKS (JP)

on:
  push:
    branches: [jp_model_lake_adapt]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, jp]
    steps:
      - name: Deploy AKS JP
        run: |
          set -e
          cd ${{ secrets.JP_DEPLOY_PATH }}/agentic_knowledge_system
          git pull origin jp_model_lake_adapt
          docker compose -f docker-compose.jp.yml up -d --build
          docker image prune -f
          echo "✅ AKS JP deployed at $(date)"
